export enum SYNC_STATUS {
  UNSYNCED = 'unsynced',
  SYNCED = 'synced',
}

export enum DatabaseActions {
  INSERTION = 'insertion',
  MODIFICATION = 'modification',
  DELETION = 'deletion',
}

export enum DB_SHARED_TABLE_NAMES {
  BANKS = '@shared/Banks',
}
